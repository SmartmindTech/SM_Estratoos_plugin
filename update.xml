<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011802</version>

        <!-- Human-readable release version -->
        <release>1.6.2</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.6.2/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.6.2</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.6.2 - IOMAD Stability Fix</h3>
            <p><strong>Complete fix for dml_read_exception errors in all Phase 2 functions</strong></p>
            <ul>
                <li><strong>Fix:</strong> All Phase 2 functions now gracefully handle IOMAD database errors</li>
                <li><strong>Fix:</strong> Removed MUST_EXIST from company table queries - uses null check instead</li>
                <li><strong>Fix:</strong> Full try-catch coverage for get_login_essentials, get_dashboard_complete, get_course_completion_bulk, get_course_stats_bulk</li>
                <li>Functions now fall back to standard Moodle mode when IOMAD tables are unavailable</li>
                <li>Debug logging added for troubleshooting IOMAD issues</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
