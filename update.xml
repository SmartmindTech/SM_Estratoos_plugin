<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2026022346</version>

        <!-- Human-readable release version -->
        <release>2.1.46</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.46/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.46</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.46 - API URL Update</h3>

            <h4>Changes</h4>
            <ul>
                <li><strong>Default API URL:</strong> Updated default backend URL from <code>api.smartlearning.com</code> to <code>api-inbox.smartlxp.com</code>.</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.45: Activation flow alignment, HMAC verification, new APIs</li>
                <li>v2.1.44: System activation: contract dates, service token expiry, manager tokens</li>
                <li>v2.1.43: Activation model separation (standard vs IOMAD)</li>
                <li>v2.1.42: Per-company API access check, restore activation after v2.1.41</li>
                <li>v2.1.40: Get enrolled users API with IOMAD validation</li>
                <li>v2.1.37: Activation flow, webhook events, token expiry, service user</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
