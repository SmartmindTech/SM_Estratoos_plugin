<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012861</version>

        <!-- Human-readable release version -->
        <release>2.0.61</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.61/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.61</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.61 - Generic SCORM: Total Slides Detection &amp; 0-Indexed Fix</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fixes position bar showing 1/1 (100% complete) for non-Storyline SCORMs</li>
                <li>Fixes 0-indexed SCORM positions being filtered out (e.g. currentSlide=0 for first slide)</li>
                <li>Fixes position bar not resetting when switching between SCORM modules</li>
            </ul>

            <h4>Changes</h4>
            <ul>
                <li>New getGenericTotalSlides() detects total slides from JS variables (totalSlides, slideCount, etc.)</li>
                <li>slidescount updated dynamically when generic detection finds a total</li>
                <li>0-indexed positions (currentSlide=0) now converted to 1-indexed for display</li>
                <li>Initial progress defaults to slide 1 when no progress data exists</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.60: Reset position bar when switching SCORM modules</li>
                <li>v2.0.59: Fix position bar dip during resume</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
