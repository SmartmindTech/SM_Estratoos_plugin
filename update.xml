<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011913</version>

        <!-- Human-readable release version -->
        <release>1.7.13</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.13/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.13</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.13 - Fix Company Search, Teacher Detection, Role Cleanup</h3>
            <p><strong>Three important fixes for company access and user role display</strong></p>

            <h4>Fixes:</h4>
            <ul>
                <li><strong>Company Search Bar:</strong> Now uses AMD module (companyaccess.js) for reliable search functionality</li>
                <li><strong>Teacher Detection:</strong> Teachers assigned at course level now show as "Teacher" instead of "Student"</li>
                <li><strong>Role Cleanup:</strong> Re-runs cleanup of system-level teacher/student/companymanager roles</li>
            </ul>

            <h4>Technical Changes:</h4>
            <ul>
                <li>company_access.php now loads AMD module instead of inline JavaScript</li>
                <li>get_user_company_roles() queries ALL role assignments (any context) to detect teachers</li>
                <li>Added multilingual teacher role detection (teacher, profesor, tutor, docente, formador, maestro)</li>
                <li>Upgrade script re-runs role cleanup for older installations</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
