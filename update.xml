<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012844</version>

        <!-- Human-readable release version -->
        <release>2.0.44</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.44/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.44</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.44 - Block Progress Until User Surpasses Tag Target</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Fixed: After 10s intercept window, score updates incorrectly increased furthestSlide</li>
                <li>Now: furthestSlide only updates when user naturally surpasses tag target</li>
                <li>Example: Jump from slide 5 to 13 via tag → progress stays at 5 until user reaches 14+</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Added naturallyBeyondTarget check: calculatedSlide > pendingSlideNavigation.slide</li>
                <li>Blocks all score updates at or below tag target after intercept window</li>
                <li>Progress resumes once user naturally navigates beyond the jumped-to slide</li>
                <li>Applies to both SCORM 1.2 and SCORM 2004 handlers</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.43: Only update furthest during natural navigation (intercept window only)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
