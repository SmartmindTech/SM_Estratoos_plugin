<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011701</version>

        <!-- Human-readable release version -->
        <release>1.5.1</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.5.1/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.5.1</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.5.1 - Conversation Messages API</h3>
            <p><strong>New function for messaging synchronization with IOMAD support</strong></p>
            <ul>
                <li><strong>local_sm_estratoos_plugin_get_conversation_messages</strong> - Fetch messages from a conversation</li>
                <li>Works with category-scoped tokens (IOMAD) - no longer requires system context</li>
                <li>Supports pagination for bulk message retrieval (up to 1000 messages per request)</li>
                <li>Includes timefrom parameter for delta sync (fetch only new messages)</li>
                <li>Returns members info with each response for efficient rendering</li>
                <li>Compatible with both IOMAD and standard Moodle installations</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
