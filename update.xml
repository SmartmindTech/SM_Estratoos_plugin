<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012600</version>

        <!-- Human-readable release version -->
        <release>1.8.0</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.8.0/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.8.0</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.8.0 - Activity Progress API</h3>

            <h4>New Features</h4>
            <ul>
                <li><strong>New API Function:</strong> <code>get_activity_progress</code> - Lightweight function to retrieve progress metadata from any activity type (SCORM, Quiz, Book, Lesson, Assignment, Page, Resource, URL)</li>
                <li>Returns slide/page/question counts, current position, score, attempts without heavy content payloads</li>
            </ul>

            <h4>Bug Fixes</h4>
            <ul>
                <li><strong>SCORM currentslide fix:</strong> Now correctly extracts slide position from <code>cmi.core.lesson_location</code> tracking data (was incorrectly returning SCO ID)</li>
                <li>Supports various lesson_location formats: numeric (5), slide_5, 1M5, 5/139</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v1.7.99: Company access logic fixes, timezone handling</li>
                <li>v1.7.98: Correct OAuth2 defaults (issuer=backend, origins=frontend)</li>
                <li>v1.7.97: Default CSP includes both inbox and api-inbox domains</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
