<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011800</version>

        <!-- Human-readable release version -->
        <release>1.6.0</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.6.0/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.6.0</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.6.0 - Phase 2: Login &amp; Dashboard Optimization</h3>
            <p><strong>Major performance improvements - Login from ~6s to &lt;500ms, Dashboard from ~20s to &lt;2s</strong></p>
            <ul>
                <li><strong>get_login_essentials</strong> - Get all login data in 1 call (replaces 6+ API calls)</li>
                <li><strong>get_dashboard_complete</strong> - Complete dashboard data in 1 call (replaces 10+ calls per Moodle)</li>
                <li><strong>get_course_completion_bulk</strong> - All users completion in 1 query (eliminates N+1 pattern)</li>
                <li><strong>get_course_stats_bulk</strong> - Multi-course statistics for teacher dashboards</li>
                <li>New server-side caches for login, dashboard, completion, and stats</li>
                <li>Full IOMAD and standard Moodle compatibility</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
