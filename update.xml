<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011805</version>

        <!-- Human-readable release version -->
        <release>1.6.5</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.6.5/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.6.5</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.6.5 - Dashboard Stats Function</h3>
            <p><strong>New API function for dashboard optimization</strong></p>
            <ul>
                <li><strong>New:</strong> local_sm_estratoos_plugin_get_dashboard_stats function</li>
                <li>Returns all dashboard statistics in a single API call</li>
                <li>Includes: courses_count, to_grade_count, deadlines_count, urgent_count</li>
                <li>Optional per-course breakdown with progress percentages</li>
                <li>Reduces dashboard loading from ~3.7s to &lt;300ms</li>
                <li>Full IOMAD and non-IOMAD support with proper exception handling</li>
                <li>1-minute caching for optimal performance</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
