<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012857</version>

        <!-- Human-readable release version -->
        <release>2.0.57</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.57/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.57</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.57 - Persist Furthest Progress in Moodle Score</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Furthest slide progress is now properly registered in Moodle's SCORM score</li>
                <li>Score no longer regresses when navigating backwards or jumping via tags</li>
                <li>Progress shows correctly on page refresh</li>
            </ul>

            <h4>Changes</h4>
            <ul>
                <li>Score write interception: cmi.core.score.raw (1.2) and cmi.score.raw (2004) never written below furthest-based score</li>
                <li>Scheduled post-init writes now also correct score in Moodle DB</li>
                <li>Initial progress message sends furthestSlide on page load for immediate correct display</li>
                <li>All changes applied to both SCORM 1.2 and SCORM 2004</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.56: Read+write interceptors for resume correction</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
