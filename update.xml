<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012885</version>

        <!-- Human-readable release version -->
        <release>2.0.85</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.85/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.85</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.85 - Fix Resume Correction for Captivate SCORM</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fix resume not navigating to furthest slide after refresh: LMSInitialize wrapper now always corrects suspend_data when furthestSlide is set, even when the initial retry loop pre-populated it or lesson_location already matched</li>
                <li>Handles case where lesson_location=4 (boosted) but suspend_data cs=2 (actual slide 3) — Captivate uses cs field, not lesson_location, for resume position</li>
                <li>Applied to both SCORM 1.2 and SCORM 2004 API wrappers</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.84: Fix Captivate position bar bugs (Generic 0-based misread, resume correction fallback)</li>
                <li>v2.0.83: Refactor tracking_js.php into modular class files by SCORM vendor</li>
                <li>v2.0.82: Fix Captivate SCORM position bar showing furthest instead of current slide</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
