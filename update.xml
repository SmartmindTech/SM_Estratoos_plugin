<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2026020932</version>

        <!-- Human-readable release version -->
        <release>2.1.31</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.31/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.31</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.31 - Document ID & Token Watcher</h3>

            <h4>New Features</h4>
            <ul>
                <li>Document ID fields (DNI/NIE/Passport) with Spain compliance validation</li>
                <li>API: Token watcher endpoint (get_new_tokens) for existing user onboarding</li>
                <li>Document fields in create_user, create_users_batch, get_new_users APIs</li>
                <li>CSV/Excel templates updated with document_type and document_id columns</li>
                <li>Notified/notified_at tracking on token metadata table</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.30: User creation APIs, batch creation, watcher endpoint</li>
                <li>v2.1.29: Calendar event update API function</li>
                <li>v2.1.28: Content centering and scrollbar cleanup in embed mode</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
