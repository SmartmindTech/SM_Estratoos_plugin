<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012720</version>

        <!-- Human-readable release version -->
        <release>2.0.20</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.20/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.20</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.20 - Fix Progress Loss from Write Interception</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Disabled suspend_data WRITE interception (was corrupting visited slides)</li>
                <li>Now only intercept READS to set initial slide position</li>
                <li>Let Storyline write its natural state to preserve progress</li>
                <li>Fixes issue where visited slides array ("v") was being reset</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Problem: Write interception confused Storyline's internal state</li>
                <li>Solution: Intercept LMSGetValue only, not LMSSetValue</li>
                <li>Result: Navigation works AND progress is preserved</li>
                <li>Both SCORM 1.2 and SCORM 2004 affected</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.19: Prevent navigation reload loops</li>
                <li>v2.0.18: Enhanced resume field modification + fallback</li>
                <li>v2.0.17: Intercept suspend_data writes (now disabled)</li>
                <li>v2.0.16: URL-based slide navigation (?slide=X) for embed</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
