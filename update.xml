<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012864</version>

        <!-- Human-readable release version -->
        <release>2.0.64</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.64/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.64</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.64 - Fix Backward Navigation &amp; Faster Detection</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fix backward navigation not updating position bar (e.g. slide 2→1 stayed at 2/5)</li>
                <li>Fix tag navigation failing because SmartLearning thought user was at wrong slide</li>
                <li>Speed up generic total slides detection (was ~6.5s, now ~2.3s)</li>
            </ul>

            <h4>Changes</h4>
            <ul>
                <li>LMSSetValue writes to lesson_location now pass as directSlide (authoritative, allows backward)</li>
                <li>Only poll-based reads suppress backward movement (stale data protection)</li>
                <li>Generic detection start delay reduced from 5000ms to 1500ms, interval from 1500ms to 800ms</li>
                <li>Same fix applied to both SCORM 1.2 (cmi.core.lesson_location) and SCORM 2004 (cmi.location)</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.63: Fix false 100% completion, totalSlides=0 for unknown totals</li>
                <li>v2.0.62: DOM-based total slides, suppress false 100%, fix initial dip</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
