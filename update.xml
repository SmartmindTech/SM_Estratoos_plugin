<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025020324</version>

        <!-- Human-readable release version -->
        <release>2.1.24</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.24/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.24</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.24 - Self-Service Profile Update API</h3>

            <h4>New Feature</h4>
            <ul>
                <li>New API: update_user — self-service profile update (mirrors core_user_update_users)</li>
                <li>Supports: name, email, phone, address, city, country, timezone, custom fields, preferences</li>
                <li>No admin capability required — token owner updates their own profile</li>
                <li>Structured error codes for email conflicts, invalid country/language, etc.</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.23: Hide activity navigation bar in embed mode</li>
                <li>v2.1.22: Hide all blocks/sidebars in embed mode across all Moodle layouts</li>
                <li>v2.1.21: Fix embed crash for feedback/survey activity types</li>
                <li>v2.1.20: User self-service APIs (update_username, update_password)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
