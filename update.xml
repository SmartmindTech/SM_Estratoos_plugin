<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012841</version>

        <!-- Human-readable release version -->
        <release>2.0.41</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.41/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.41</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.41 - Persistent lesson_location Interception</h3>

            <h4>Changes</h4>
            <ul>
                <li>lesson_location intercept now has NO time limit</li>
                <li>Interception continues for entire SCORM session</li>
                <li>Poll reads lesson_location - must return target slide</li>
                <li>pendingSlideNavigation preserved after suspend_data window closes</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Removed time window check from lesson_location/location intercept</li>
                <li>suspend_data still uses time-limited window</li>
                <li>pendingSlideNavigation no longer nulled when suspend_data window closes</li>
                <li>Ensures consistent slide position for entire session</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.40: Added lesson_location interception (time-limited)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
