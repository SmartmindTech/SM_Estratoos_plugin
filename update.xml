<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012859</version>

        <!-- Human-readable release version -->
        <release>2.0.59</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.59/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.59</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.59 - Fix Position Bar Dip During Resume</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Fixes position bar briefly dipping (e.g. 16 → 13 → 16) during resume initialization</li>
                <li>Root cause: poll reads stale lesson_location after resume intercept window expires, causing lastSlide to decrease temporarily</li>
            </ul>

            <h4>Changes</h4>
            <ul>
                <li>sendProgressUpdate now only allows directSlide (from suspend_data) to decrease lastSlide</li>
                <li>Poll-based lesson_location updates can only increase lastSlide, not decrease it</li>
                <li>Stale backward movements from polls are suppressed with a log message</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.58: On-the-fly resume correction (fixes defineProperty trap timing issue)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
