<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012181</version>

        <!-- Human-readable release version -->
        <release>1.7.81</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.81/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.81</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.81 - JWKS Table Index Fix</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Fixed JWKS cache table installation error on MySQL databases</li>
                <li>MySQL has a 255 character limit for indexed fields - changed issuer_url from indexed CHAR(512) to TEXT</li>
                <li>Added issuer_url_hash (SHA256, 64 chars) column for unique indexing</li>
            </ul>

            <h4>Previous Version (1.7.80) Features</h4>
            <ul>
                <li>OAuth2/OIDC JWT validation for chromeless iframe embedding</li>
                <li>JWKS caching for efficient public key retrieval</li>
                <li>Chromeless activity renderer for seamless SmartLearning integration</li>
                <li>Support for all activity types: SCORM, Quiz, Assignment, Lesson, Book, Page, Resource, URL, Folder</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
