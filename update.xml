<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011920</version>

        <!-- Human-readable release version -->
        <release>1.7.20</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.20/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.20</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.20 - FIX: Company Search Now Works!</h3>
            <p><strong>Fixed Bootstrap CSS override that prevented search filtering</strong></p>

            <h4>Root Cause:</h4>
            <p>The company items used Bootstrap's <code>d-flex</code> class which sets
            <code>display: flex !important</code>. This overrode the inline
            <code>style.display = "none"</code> that JavaScript was setting.</p>

            <h4>Solution:</h4>
            <p>Changed from <code>item.style.display = "none"</code> to using Bootstrap's
            <code>d-none</code> class which has <code>display: none !important</code>
            and properly overrides <code>d-flex</code>.</p>

            <h4>Now Working:</h4>
            <ul>
                <li>Search filtering as you type</li>
                <li>Select All / Deselect All (only affects visible items)</li>
                <li>Enabled/Disabled badges</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
