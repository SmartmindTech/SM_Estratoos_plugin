<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012602</version>

        <!-- Human-readable release version -->
        <release>1.8.2</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.8.2/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.8.2</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.8.2 - Real-time SCORM Progress via PostMessage</h3>

            <h4>New Features</h4>
            <ul>
                <li><strong>PostMessage real-time progress:</strong> SCORM player now sends progress updates via PostMessage to parent window</li>
                <li>SmartLearning can receive instant slide changes without polling the API</li>
                <li>Tracks: currentSlide, totalSlides, lessonStatus, score, progressPercent</li>
                <li>Supports both SCORM 1.2 (LMSSetValue) and SCORM 2004 (SetValue) APIs</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v1.8.1: SCORM currentslide fix (filter by primary SCO)</li>
                <li>v1.8.0: New <code>get_activity_progress</code> API function</li>
                <li>v1.7.99: Company access logic fixes, timezone handling</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
