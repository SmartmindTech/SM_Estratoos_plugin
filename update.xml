<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012832</version>

        <!-- Human-readable release version -->
        <release>2.0.32</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.32/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.32</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.32 - Anti-Reload-Loop with Separate Tracking Key</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>New scorm_fallback_reload_ sessionStorage key for tracking reload attempts</li>
                <li>Blocks reload if same slide attempted within 15 seconds</li>
                <li>Poll fallback now allows reload (anti-loop protection in place)</li>
                <li>Clear markers on success for future navigations</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Separate key from scorm_pending_navigation_ (that one gets cleared on read)</li>
                <li>modifySuspendDataAndReload checks fallback status before reload</li>
                <li>Clear fallback marker when position matches target</li>
                <li>Clear fallback marker when new navigation starts</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.31: Add skipReload param to navigateToSlide</li>
                <li>v2.0.30: Re-enable direct navigation fallback</li>
                <li>v2.0.29: Comprehensive suspend_data field modification</li>
                <li>v2.0.28: PHP-level suspend_data modification</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
