<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012605</version>

        <!-- Human-readable release version -->
        <release>1.8.5</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.8.5/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.8.5</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.8.5 - Capture suspend_data for Articulate Storyline</h3>

            <h4>Enhancements</h4>
            <ul>
                <li><strong>Articulate Storyline support:</strong> PostMessage now captures <code>cmi.suspend_data</code> which is where Articulate Storyline stores slide position</li>
                <li>Parses JSON suspend_data formats (currentSlide, slide, resume fields)</li>
                <li>Parses scene_slide format patterns (e.g., "1_5" = scene 1, slide 5)</li>
                <li>Added <code>lastSlide</code> tracking to avoid duplicate messages</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v1.8.4: Fix PostMessage parameter extraction (use 'a' param)</li>
                <li>v1.8.3: Fix update notification URL to use relative path</li>
                <li>v1.8.2: PostMessage real-time SCORM progress tracking</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
