<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012177</version>

        <!-- Human-readable release version -->
        <release>1.7.77</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.77/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.77</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.77 - Improved SCORM Slide Detection</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Added generic slide file detection as fallback for unknown authoring tools</li>
                <li>Now detects slides from patterns like <code>/res/data/slideXX.js</code> (Genially, custom tools)</li>
                <li>Also detects scene and page patterns used by various tools</li>
            </ul>

            <h4>Detection Patterns Added</h4>
            <ul>
                <li><code>/res/data/slide*.js</code> - Generic HTML5 tools</li>
                <li><code>/slides/slide*.html</code> - Custom SCORM builders</li>
                <li><code>/scenes/scene*.js</code> - Scene-based tools</li>
                <li><code>/pages/page*.html</code> - Page-based tools</li>
            </ul>

            <h4>New Detection Method</h4>
            <ul>
                <li><code>slidedetection: "generic_slide_files"</code> - When slides detected from file patterns</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
