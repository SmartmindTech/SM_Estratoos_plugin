<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011924</version>

        <!-- Human-readable release version -->
        <release>1.7.24</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.24/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.24</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.24 - Fix User Functions Capability for Company-Scoped Tokens</h3>
            <p><strong>Fixed "nopermissions" error for get_users_by_field and get_users functions</strong></p>

            <h4>Problem:</h4>
            <p>Company-scoped tokens were failing with "Lo sentimos, pero no tiene los permisos para hacer esto
            (Ver perfiles de usuario)" when calling user lookup functions for chat/messaging features.</p>

            <h4>Solution:</h4>
            <p>Removed the <code>moodle/user:viewdetails</code> capability requirement. Security is now
            enforced by company filtering - users can only see other users in their own company.</p>

            <h4>Affected Functions:</h4>
            <ul>
                <li><code>local_sm_estratoos_plugin_get_users_by_field</code></li>
                <li><code>local_sm_estratoos_plugin_get_users</code></li>
            </ul>

            <h4>Impact:</h4>
            <p>Chat user lists and user info lookups now work correctly with company-scoped tokens.</p>
            ]]>
        </releasenotes>
    </update>
</updates>
