<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012842</version>

        <!-- Human-readable release version -->
        <release>2.0.42</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.42/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.42</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.42 - Restore furthestSlide from sessionStorage</h3>

            <h4>Changes</h4>
            <ul>
                <li>Initialize furthestSlide from sessionStorage on load</li>
                <li>Prevents progress reset on iframe reload</li>
                <li>Uses key scorm_furthest_slide_{cmid}</li>
                <li>Frontend stores true maximum in sessionStorage</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Storyline writes low init scores when starting</li>
                <li>Without persistence, furthestSlide resets to null on reload</li>
                <li>sessionStorage persists across iframe reloads</li>
                <li>Graceful fallback if sessionStorage unavailable</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.41: Persistent lesson_location interception (no time limit)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
