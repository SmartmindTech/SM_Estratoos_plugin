<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012838</version>

        <!-- Human-readable release version -->
        <release>2.0.38</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.38/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.38</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.38 - Fast Polling for SCORM API Wrap</h3>

            <h4>Critical Fix</h4>
            <ul>
                <li>Fast 10ms polling for first 500ms when navigation pending</li>
                <li>Storyline reads suspend_data IMMEDIATELY after API creation</li>
                <li>Previous 200ms interval was too slow - API missed</li>
                <li>Set iframe.onload BEFORE iframe.src to ensure handler fires</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>lib.php: 10ms polling during critical 500ms window</li>
                <li>lib.php: Switch to 200ms after critical window</li>
                <li>embed_renderer.php: Consolidated into single IIFE</li>
                <li>embed_renderer.php: Set src last after all handlers attached</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.37: Defer iframe with setTimeout(0) after verify</li>
                <li>v2.0.36: Fix URL escaping for JavaScript context</li>
                <li>v2.0.35: Fix race condition with iframe creation</li>
                <li>v2.0.34: Disable direct navigation fallback</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
