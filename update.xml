<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025020301</version>

        <!-- Human-readable release version -->
        <release>2.1.1</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.1/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.1</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.1 - Auto-Start Quiz Attempt on Tag Navigation</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Quiz tag navigation now auto-starts a new attempt when no in-progress attempt exists, ensuring direct navigation to the tagged question</li>
                <li>Respects quiz access rules (max attempts, time windows) when auto-starting attempts</li>
            </ul>

            <h4>Previous Version (v2.1.0)</h4>
            <ul>
                <li>Quiz embed redirects directly to attempt.php with targetSlide/furthestSlide</li>
                <li>Forum redirect support with displayHint='redirect-forum' and redirectPath</li>
                <li>Label inline display with displayHint='inline-content'</li>
                <li>SmartLearning design system CSS for all embedded activities</li>
            </ul>

            <h4>Earlier Versions</h4>
            <ul>
                <li>v2.0.99: Whole-activity tagging + activity-progress message type</li>
                <li>v2.0.98: Activity position tracking for quiz, book, lesson</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
