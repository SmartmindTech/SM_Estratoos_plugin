<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025020305</version>

        <!-- Human-readable release version -->
        <release>2.1.5</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.5/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.5</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.5 - Quiz Class Fix + Folder CSS + Activity Handling</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fixed "Class quiz not found" error - now supports both Moodle 4.0+ (mod_quiz\quiz) and older versions</li>
                <li>Improved folder file listing CSS - cleaner table appearance, removed tree connector lines</li>
                <li>Explicit handling for feedback, data, workshop, choice, survey, glossary, wiki, chat, folder activities</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.4: Label progress + manual completion + view tracking</li>
                <li>v2.1.3: Fix URL embed constant + scrollbar CSS</li>
                <li>v2.1.2: Fix quiz attempt start path</li>
                <li>v2.1.1: Auto-start quiz attempt on tag navigation</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
