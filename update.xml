<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012898</version>

        <!-- Human-readable release version -->
        <release>2.0.98</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.98/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.98</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.98 - Activity Position Tracking for Quiz, Book, and Lesson</h3>

            <h4>New Feature</h4>
            <ul>
                <li>Real-time position tracking for quiz (pages), book (chapters), and lesson (pages) activities via postMessage — enables position bar, tagging, and go-back button in SmartLearning</li>
                <li>Position-aware redirect URLs in embed renderer: clicking a tag navigates directly to the correct quiz page, book chapter, or lesson page</li>
                <li>New activity/tracking_js class generates per-activity tracking JS that reuses the scorm-progress message format for full frontend compatibility (zero frontend changes needed)</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.97: Extend embed mode to all activity types (hide Moodle chrome for quiz, assignment, lesson, book, etc.)</li>
                <li>v2.0.96: Fix iSpring modern navigation + suspend_data parsing for Rise 360/iSpring Base64</li>
                <li>v2.0.95: Fix refresh resume for all vendors via LMSInitialize wrapper + iSpring post-load navigation</li>
                <li>v2.0.94: Fix suspend_data corruption + resume format for iSpring/Rise 360</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
