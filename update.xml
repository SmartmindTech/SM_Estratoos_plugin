<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025020320</version>

        <!-- Human-readable release version -->
        <release>2.1.20</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.20/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.20</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.20 - User Self-Service APIs</h3>

            <h4>New Features</h4>
            <ul>
                <li>New API: update_username — users can change their own username</li>
                <li>New API: update_password — users can change their own password (requires current password)</li>
                <li>Both functions are self-service (no admin capability needed)</li>
                <li>Auth plugin validation (blocks LDAP/CAS/SSO users from changing credentials)</li>
                <li>Moodle password policy enforcement for password changes</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.19: Lesson keyword matching for page title detection</li>
                <li>v2.1.15: Quiz page mapping + CSS fixes</li>
                <li>v2.1.12: Quiz tag navigation uses review mode</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
