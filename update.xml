<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011935</version>

        <!-- Human-readable release version -->
        <release>1.7.35</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.35/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.35</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.35 - Fix Timezone in Expiry Date Picker</h3>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fixed timezone mismatch in company expiry date picker</li>
                <li>JavaScript now uses UTC ("Z" suffix) when converting dates to timestamps</li>
                <li>PHP now uses gmdate() instead of date() for consistent UTC handling</li>
                <li>This fixes the 1-day shift issue when server timezone differs from browser timezone</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li><strong>v1.7.34:</strong> Fixed context validation in session APIs</li>
                <li><strong>v1.7.33:</strong> Added missing get_company_id_from_token() method</li>
                <li><strong>v1.7.32:</strong> Fixed context validation in 9 API functions for non-IOMAD users</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
