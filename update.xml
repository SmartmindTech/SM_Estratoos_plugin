<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012855</version>

        <!-- Human-readable release version -->
        <release>2.0.55</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.55/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.55</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.55 - Correct Resume Position to Furthest Slide</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>On resume, Storyline now loads at furthest slide (not tag target from previous session)</li>
                <li>Fixes "goes to 13 even though 16 is the furthest" after page refresh</li>
            </ul>

            <h4>Changes</h4>
            <ul>
                <li>On session start: if sessionStorage furthest > DB slide, correct backing store before Storyline reads it</li>
                <li>After tag navigation intercept window: scheduled write ensures DB has furthest slide</li>
                <li>Both fixes applied to SCORM 1.2 and SCORM 2004</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.54: Write interceptor phases (tag target during init, furthest after)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
