<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012850</version>

        <!-- Human-readable release version -->
        <release>2.0.50</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.50/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.50</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.50 - Fix Race Condition with Navigation Starting Signal</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Fixed: OLD iframe could still write wrong slide before NEW navId was set</li>
                <li>Added 'navigation_starting' signal that runs IMMEDIATELY when embed.php loads</li>
                <li>OLD iframe now checks this signal and stops intercepting if newer navigation started</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>embed_renderer.php sets scorm_navigation_starting_{cmid} FIRST (before iframe loads)</li>
                <li>isOurNavigationStillActive() now checks TWO things:</li>
                <li>1. scorm_current_navigation_ (set by player.php - may be too late)</li>
                <li>2. scorm_navigation_starting_ (set by embed.php - catches race condition)</li>
                <li>Compares timestamps to detect if newer navigation started</li>
            </ul>

            <h4>Race Condition Timeline</h4>
            <ul>
                <li>T0: User clicks navigation</li>
                <li>T1: embed.php loads, sets navigation_starting signal IMMEDIATELY</li>
                <li>T2: OLD iframe writes, checks signal, sees newer timestamp → STOPS intercepting</li>
                <li>T3: player.php loads, sets scorm_current_navigation_</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.49: navId generated in embed_renderer.php</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
