<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011926</version>

        <!-- Human-readable release version -->
        <release>1.7.26</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.26/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.26</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.26 - Non-IOMAD Support & Category-Scoped Tokens Security</h3>
            <p><strong>Two security and compatibility improvements</strong></p>

            <h4>1. Non-IOMAD Support for Manager Tokens API</h4>
            <p>The <code>get_company_manager_tokens_status</code> function now works on non-IOMAD Moodles:</p>
            <ul>
                <li><strong>IOMAD mode:</strong> Returns managers for a specific company (managertype > 0)</li>
                <li><strong>Non-IOMAD mode:</strong> Returns all users with manager-like roles who have tokens</li>
            </ul>
            <p>Manager detection via role patterns: manager, admin, gerente, administrador</p>

            <h4>2. Category-Scoped Tokens (Security Improvement)</h4>
            <p>Non-IOMAD tokens for normal users now use <code>CONTEXT_COURSECAT</code> (level 40) instead of
            <code>CONTEXT_SYSTEM</code> (level 10):</p>
            <ul>
                <li><strong>Super admins:</strong> Keep system context (full access)</li>
                <li><strong>Normal users:</strong> Category context (restricted access)</li>
            </ul>
            <p>Existing tokens are automatically migrated during upgrade.</p>
            ]]>
        </releasenotes>
    </update>
</updates>
