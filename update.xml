<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012839</version>

        <!-- Human-readable release version -->
        <release>2.0.39</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.39/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.39</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.39 - Revert to v2.0.34 Behavior</h3>

            <h4>Revert</h4>
            <ul>
                <li>Reverted to v2.0.34 codebase</li>
                <li>Removed changes from v2.0.35-v2.0.38 (caused issues)</li>
                <li>Direct navigation fallback disabled</li>
                <li>JS intercepts work correctly without extra complexity</li>
            </ul>

            <h4>Stable Behavior</h4>
            <ul>
                <li>Disabled direct navigation fallback</li>
                <li>Fallback was causing unnecessary double-loads</li>
                <li>cmi.core.lesson_location updates slower than visual display</li>
                <li>Simplifies code and prevents reload loops</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.0.38: Fast polling (reverted)</li>
                <li>v2.0.37: setTimeout defer (reverted)</li>
                <li>v2.0.36: URL escaping fix (reverted)</li>
                <li>v2.0.35: Race condition fix (reverted)</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
