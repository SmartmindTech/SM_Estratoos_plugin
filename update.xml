<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011700</version>

        <!-- Human-readable release version -->
        <release>1.5.0</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.5.0/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.5.0</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.5.0 - Bulk Data Optimization</h3>
            <p><strong>Major performance improvements to eliminate N+1 query patterns</strong></p>
            <ul>
                <li><strong>get_all_users_bulk</strong> - Bulk user fetch with embedded roles and pagination (5000 users &lt; 2s)</li>
                <li><strong>get_dashboard_summary</strong> - Single-call dashboard data replacing 5-10 API calls (&lt; 500ms)</li>
                <li><strong>get_courses_with_progress_bulk</strong> - Bulk courses with completion and grades (500 courses &lt; 3s)</li>
                <li><strong>get_changes_since</strong> - Delta sync for incremental updates (70-90% data transfer reduction)</li>
                <li><strong>health_check_extended</strong> - Extended health check with summary statistics (&lt; 200ms)</li>
                <li><strong>Server-side caching</strong> - Moodle Cache API integration with event-based invalidation</li>
                <li><strong>IOMAD compatible</strong> - All functions support both IOMAD and standard Moodle installations</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
