<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025012845</version>

        <!-- Human-readable release version -->
        <release>2.0.45</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.0.45/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.0.45</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.0.45 - Sync lesson_location with suspend_data</h3>

            <h4>Bug Fix</h4>
            <ul>
                <li>Fixed: Storyline loading at stale lesson_location instead of suspend_data position</li>
                <li>On tag navigation: update BOTH suspend_data AND lesson_location in database</li>
                <li>On normal load: sync lesson_location with suspend_data slide value</li>
            </ul>

            <h4>Technical Details</h4>
            <ul>
                <li>Added update_lesson_location() to set database value for target slide</li>
                <li>Added sync_lesson_location_with_suspend_data() for normal loads</li>
                <li>Added getSlideFromSuspendData() helper to parse slide from LZ-compressed data</li>
                <li>Supports both Moodle 4.x normalized tables and legacy scorm_scoes_track</li>
            </ul>

            <h4>Previous Version</h4>
            <ul>
                <li>v2.0.44: Block furthestSlide updates until user surpasses tag target</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
