<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025020312</version>

        <!-- Human-readable release version -->
        <release>2.1.12</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v2.1.12/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v2.1.12</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 2.1.12 - Quiz Tag Navigation in Review Mode</h3>

            <h4>New Features</h4>
            <ul>
                <li>Quiz tag navigation now uses REVIEW MODE (read-only)</li>
                <li>Clicking tagged question shows it without starting new attempt</li>
                <li>Progress reflects best/last grade based on quiz settings</li>
                <li>Works even when max attempts exhausted</li>
            </ul>

            <h4>Bug Fixes</h4>
            <ul>
                <li>Fixed button CSS - outline buttons now styled consistently</li>
                <li>Lesson position tracking keeps position on answer pages</li>
            </ul>

            <h4>Previous Versions</h4>
            <ul>
                <li>v2.1.11: Lesson SQL fix + page scroll + fallback protection</li>
                <li>v2.1.10: Lesson position tracking via iframe wrapper</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
