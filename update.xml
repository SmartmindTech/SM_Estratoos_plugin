<?xml version="1.0" encoding="UTF-8"?>
<!--
    Moodle plugin update server configuration file.

    This file is checked by Moodle to determine if updates are available.
    When you release a new version:
    1. Update the version number in version.php
    2. Update this file with the new version details
    3. Create a GitHub release with the ZIP file
    4. Commit and push both files

    GitHub Repository: https://github.com/SmartmindTech/SM_Estratoos_plugin
-->
<updates>
    <update>
        <!-- Plugin identifier (must match $plugin->component in version.php) -->
        <component>local_sm_estratoos_plugin</component>

        <!-- Version number (must match $plugin->version in version.php) -->
        <version>2025011907</version>

        <!-- Human-readable release version -->
        <release>1.7.7</release>

        <!-- Minimum required Moodle version -->
        <requires>2022112800</requires>

        <!-- Maturity level: MATURITY_ALPHA=50, MATURITY_BETA=100, MATURITY_RC=150, MATURITY_STABLE=200 -->
        <maturity>200</maturity>

        <!-- URL to download the plugin ZIP file (use GitHub releases) -->
        <download>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/download/v1.7.7/sm_estratoos_plugin.zip</download>

        <!-- Optional: URL for more information about this update -->
        <url>https://github.com/SmartmindTech/SM_Estratoos_plugin/releases/tag/v1.7.7</url>

        <!-- Release notes (keep it brief) -->
        <releasenotes>
            <![CDATA[
            <h3>Version 1.7.7 - Bug Fixes</h3>
            <p><strong>Critical fixes for token management UI</strong></p>
            <ul>
                <li><strong>Fix:</strong> Suspended tokens now appear in the token list (was hidden due to INNER JOIN)</li>
                <li><strong>Fix:</strong> Functions selection box now has fixed size (300px height)</li>
                <li><strong>Fix:</strong> Suspended tokens show user info from backup data</li>
                <li><strong>Fix:</strong> Token linking when re-enabling companies with existing tokens</li>
                <li>Cache purge on upgrade to ensure AMD modules reload</li>
            </ul>
            <h4>Included from v1.7.x:</h4>
            <ul>
                <li>Token suspension backup/restore with same hash (v1.7.5-1.7.6)</li>
                <li>Automatic system-level role assignment for new users</li>
                <li>Company access control for IOMAD multi-tenant</li>
            </ul>
            ]]>
        </releasenotes>
    </update>
</updates>
